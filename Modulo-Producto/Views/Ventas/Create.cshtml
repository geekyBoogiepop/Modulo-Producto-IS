@model Modulo_Producto.Models.Venta

@{
    ViewData["Title"] = "Nueva Venta";
    var productList = ViewBag.ProductList as SelectList;
}

<h1 class="text-center"><span class="highlight">Nueva Venta</span></h1>

<div class="row">
    <div class="col-md-4">
        @using (Html.BeginForm("Create", "Ventas", FormMethod.Post))
        {
            <div class="mb-2">
                <label for="Cliente">Cliente</label>
                <input type="text" name="Cliente" id="Cliente" class="form-control" />
            </div>

            <div class="mb-2">
                <label for="Caja">Caja</label>
                <input type="text" name="Caja" id="Caja" class="form-control" />
            </div>

            <div class="mb-2">
                <label for="IdProducto">Producto:</label>
                @Html.DropDownList("IdProducto", productList, "-- Seleccionar --", new { @class = "form-control" })
            </div>

            <div class="mb-2">
                <label for="Cantidad">Cantidad</label>
                <input type="number" name="Cantidad" id="Cantidad" class="form-control" />
            </div>

            <input type="submit" value="Guardar" class="btn btn-primary" />
            <a class="btn btn-warning" asp-action="Index">Regresar</a>
        }
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
